export type ClothingItem = {
  userId: string;
  imgUrl: string;
  id: number;
  name: string | null;
  brand: string | null;
  category: string | null;
};

// UI
export type GridImageSize = "S" | "M" | "L";
